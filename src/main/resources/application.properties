spring.application.name=demo
server.port=8081

# MySQL Connection Configuration - Pointing to port 3307 (our new tunnel)
spring.datasource.url=jdbc:mysql://127.0.0.1:3307/B-PHORN_Sophath-db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=springuser
spring.datasource.password=Hello@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# File upload settings
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB
app.storage.photo-dir=${PHOTO_UPLOAD_DIR:uploads/photos}
app.verification-base-url=${VERIFICATION_BASE_URL:http://localhost:8080/verify}